name: 'Fetch news'

on:
  schedule:
    - cron: "0 6,18 * * *"
  push:
    branches:
      - main 
      - testing

jobs:
  deploy:
    name: 'Deploy'
    runs-on: ubuntu-latest

    steps:
      - name: Clone the repository
        run: git clone git@github.com:responsible-ai-collaborative/nlp-monitoring.git

      - name: Test if it worked
        run: curl http://lmcnulty.me/nlp-monitoring-running

